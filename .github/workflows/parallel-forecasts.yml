name: Parallel Forecast Generation

on:
  schedule:
    - cron: '0 1 1 * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "Workflow is working"
